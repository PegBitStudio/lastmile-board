# This repo is published to GitHub Pages — anything committed here is public.
# Voice recordings and other working files are dropped in this folder by hand,
# so keep them out of git by default.

*.aac
*.ogg
*.mp3
*.m4a
*.wav
*.opus

# Office lock files
~$*
